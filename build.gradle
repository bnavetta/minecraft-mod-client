allprojects {
	apply plugin: 'java'
	apply plugin: 'groovy'
	apply plugin: 'eclipse'

	repositories {
		mavenCentral()
	}
	
	project.ext {
		springVersion = '3.1.2.RELEASE'
		groovyVersion = '1.8.7'
	}
}

task wrapper(type:Wrapper) {
	gradleVersion = '1.0-rc-3'
}
